version: "3.3"

services:
  test-openappsec:
    image: test/openappsec
    ipc: host
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    networks:
      - traefik-network

networks:
  traefik-network:
    external: true
